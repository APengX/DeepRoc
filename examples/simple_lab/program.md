# Simple lab

Improve `message.txt` so that the evaluator score goes up.

Rules:
- Only edit files inside the mutable scope.
- Prefer small, reviewable changes.
- The score is the number of times the word `agent` appears.
- Return a concise rationale and deterministic edits.
